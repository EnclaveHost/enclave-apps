[package]
name = "gs-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
openssl = "0.10"
# Inflating the app's /display bands (raw deflate). Pure Rust, no C.
miniz_oxide = "0.8"

[build-dependencies]
cc = "1"

[profile.release]
opt-level = 2
