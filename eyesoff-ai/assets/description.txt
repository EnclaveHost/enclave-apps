Private LLM chat on attested GPUs. Prompts and replies are processed only inside the enclave; the operator and the host never see your conversation. Chat history lives only in your browser, so there is nothing server-side to leak, subpoena, or breach. OpenAI-compatible API with streaming: set an API key in config and point any OpenAI SDK at your deployment URL, or use the built-in playground. Ships no weights. Attach verified model volumes and one deployment serves whatever fits, largest first.
